import type { Coupon } from "@/lib/data/types";

// No coupons in the bundled mock data — the admin creates them from
// /admin/coupons the same way categories/products are seeded once
// there's something to manage.
export const coupons: Coupon[] = [];
